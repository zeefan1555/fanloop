# 使用小而稳定的退出码集合

Fanloop 只定义 `0` 成功、`1` 业务或外部操作失败、`2` 输入错误、`4` 网络错误和 `5` 内部损坏；细粒度原因由 JSON 的稳定 `error.type` 与 `error.code` 表达。当前产品不拥有认证、内容安全或高风险确认，因此不复制 Lark CLI 的 `3/6/10`。上游 Lark CLI 认证失败归入 `1`，同时保留类型化上游错误和原退出码。
