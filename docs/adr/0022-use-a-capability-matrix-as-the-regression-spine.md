# 使用能力清单串联迁移差分与长期回归

`tests/capabilities.json` 必须让旧 Driver 的每个测试唯一归属于长期契约、Go 单测或带 ADR 的废弃能力。迁移期从同一夹具分别运行 Python 与 Go，比较退出码、结构化输出、完整本地文件变化和外部调用记录，只归一化路径、时间及 Event ID；契约优化后改用人工审核的 Golden，CI 不自动更新预期。当前规模下每个 MR 运行全部确定性门禁，不按改动目录切片；真实飞书 E2E 等 Fanloop 拥有远端传输后再加入非默认门禁。
