---
name: technical-solution-benefits
description: 将已批准方案映射为可验证效果、业务价值、技术价值和复用价值。用于 technical-solution-design 的方案收益 Step；不虚构上线结果。
---

# 评估方案收益

读取已批准的 `01-background.md` 至 `06-key-solutions.md`。逐项目标建立“基线—手段—验证—结果”
映射，并区分：

1. 已测效果：给出环境、样本、时间窗口、指标口径和结果；
2. 预期效果：给出测算依据、验证计划和达标阈值，不写成既成事实；
3. 业务价值：对用户体验、资金安全、业务规模、交付速度或成本的可验证影响；
4. 技术价值：性能、可用性、一致性、复杂度、维护成本和演进能力；
5. 适用边界：适用场景、不适用场景、前置条件、方案代价与关键取舍；
6. 复用价值：可复用边界和接入成本。

不用“显著提升”“大幅降低”等主观形容词替代数据。每项价值必须回扣目标和具体手段，无法量化
时写明验证方式和负责人。所有数字标明来源和证据状态，明确区分现状事实/已测、估算、目标与
待确认。

将结果写入并回读 `.technical-solution/sections/07-benefits.md`。片段不得出现 `#` 或 `##`；允许按
真实内容使用 `###`，但禁止 `####` 及手工 `1.1` 式标题。优先用效果表和价值映射表表达。

发现上游结论需变更时只上报最早受影响的 feedback Condition；否则上报
`solution_benefits_defined` 和片段路径。
