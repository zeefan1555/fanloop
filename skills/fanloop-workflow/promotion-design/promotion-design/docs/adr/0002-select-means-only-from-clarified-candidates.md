# 选定手段只能来自澄清确认的候选手段

`promotion-design` 必须完整消费 Context 中已确认的 `U → G → P → H`，保持这些 ID 和语义不变，通过调研取舍把候选手段 `H` 转成选定手段 `M`，并显式记录 `M ← H → G/P`；未选中的 `H` 保留拒绝理由。

不允许方案设计静默新增目标、问题或候选手段，因为这会绕过用户确认并让反馈切片失去稳定基线。若调研发现新的 `U/G/P/H`，必须回流需求澄清；最终文档仍只保留选型结果和正文，不新增独立分析产物。
