# 方案写作只展开已确认的递归闭环

`promotion-design` 完整消费 Context 中已确认的 `U/G/P/S/M`，保持节点、语义和依赖不变；
只为叶子 `M` 补齐执行闭环 `L`、效果 `E`、规划 `F` 和可读叙事。调研发现新需求节点、需要
改变解法或手段树时必须回流澄清。

这取代 ADR-0002 中“由写作阶段把候选 `H` 取舍成 `M`”的边界，避免已确认的正向/反向
闭环在下游被重新解释。
