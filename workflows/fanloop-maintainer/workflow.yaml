schema_version: 7
id: fanloop-maintainer

stages:
  - id: define
    name: Define
    jobs:
      - id: define
        name: Define
        steps:
          - {id: define_verification_contract, name: 目标与验收契约, executor: agent}

  - id: build
    name: Build
    jobs:
      - id: build
        name: Build
        steps:
          - {id: build_until_verified, name: 实现与自主验证, executor: agent}

  - id: certify
    name: Certify
    jobs:
      - id: certify
        name: Certify
        steps:
          - {id: certify_candidate, name: 独立候选认证, executor: agent}

  - id: deliver
    name: Deliver
    jobs:
      - id: deliver
        name: Deliver
        steps:
          - {id: merge_and_update_local, name: PR 合码与本地更新, executor: agent}
