schema_version: 7
id: promotion-design

stages:
  - id: requirements
    name: 需求定义
    jobs:
      - id: requirements
        name: 需求定义
        steps:
          - {id: clarify_requirements, name: 需求澄清, executor: agent}
          - {id: confirm_requirements, name: 需求人工确认, executor: human}

  - id: design
    name: 晋升方案
    jobs:
      - id: design
        name: 晋升方案
        steps:
          - {id: write_promotion_design, name: 晋升方案写作, executor: agent}
          - {id: review_promotion_design, name: 陌生评委审校, executor: agent}
          - {id: confirm_promotion_design, name: 方案人工确认, executor: human}
